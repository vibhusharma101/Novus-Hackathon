# Plan: EPRx Exchange

_Generated by /vii-plan-eng on 2026-06-08. Brief: .vii/think/eprx-exchange.md._

## Scope confirmation
- **Success metric:** the 90-second live demo lands — Seller lists in Window A → row appears in Window B's order book with no refresh → Buyer checks out → Seller's vault lights up with the order + 48h escrow timer.
- **Riskiest assumption:** Supabase Realtime over RLS-filtered Postgres changes syncs reliably and fast enough to demo live across two authenticated windows.

## Repo survey
- Next.js **16.2.7** (App Router), React **19.2.4**, TypeScript 5. `AGENTS.md` warns this Next version has breaking changes vs training data — **read `node_modules/next/dist/docs/` before writing route/server-action code.**
- Auth wired: Clerk 7.4.2, middleware protects all non-public routes, `ClerkProvider` inside `<body>`, shadcn theme via `@clerk/ui`.
- Data spine ready: `src/lib/supabase.ts` exposes `supabaseAdmin` (service role) + `createUserClient()` (Clerk-JWT scoped, RLS-enforced). `/api/chat` already secured (auth guard + zod).
- **No test framework exists.** No `test` script, no test files. Phase 0 must add Vitest + RTL + Playwright.
- **Supabase is not provisioned** — `.env.local` still has placeholder `SUPABASE_URL` / keys. Real project + Clerk→Supabase JWT template + Realtime replication are prerequisites (see Open questions).
- shadcn/ui component library fully installed; sidebar + dashboard shell exist as placeholders.

## Change list

### Phase 0 — Foundation (test harness + schema + realtime)
| Path | Action | Purpose | LOC |
|---|---|---|---|
| `package.json` | edit | Add `vitest`, `@testing-library/react`, `jsdom`, `@playwright/test`; `test`/`test:e2e` scripts | ~15 |
| `vitest.config.ts` | new | Vitest + jsdom + path aliases | ~20 |
| `playwright.config.ts` | new | Two-context e2e config, baseURL localhost | ~25 |
| `supabase/schema.sql` | new | Tables + RLS policies + realtime publication (see Data model) | ~180 |
| `src/lib/db/types.ts` | new | TS types mirroring tables | ~80 |

### Phase 1 — Pure logic (TDD, no UI)
| Path | Action | Purpose | LOC |
|---|---|---|---|
| `src/lib/epr/constants.ts` | new | Targets (R30/F20/M15), cost ranges, 5% fee, 48h TTL | ~25 |
| `src/lib/epr/liability.ts` | new | liability = market×target; deficit; cost-range bands | ~50 |
| `src/lib/epr/pricing.ts` | new | credits cost, 5% platform fee, total | ~30 |
| `src/lib/epr/matching.ts` | new | match buyer deficit → recycler pool (cap at listing qty) | ~50 |
| `src/lib/epr/orders.ts` | new | state machine pending→transferred/declined/expired + expiry | ~60 |
| `src/lib/epr/certificate.ts` | new | reference-ID generation, cert metadata | ~40 |
| `src/lib/epr/__tests__/*.test.ts` | new | Unit tests for all of the above (test-first) | ~300 |

### Phase 2 — Buyer flow
| Path | Action | Purpose | LOC |
|---|---|---|---|
| `src/app/page.tsx` | edit | Landing: copy matrix, trust badges, mock ticker, CTAs | ~120 |
| `src/app/onboarding/buyer/page.tsx` | new | Business profile form → `brands` row + role metadata | ~140 |
| `src/app/(dashboard)/calculator/page.tsx` | new | 3-step calculator (type → weights → snapshot) | ~220 |
| `src/app/(dashboard)/dashboard/page.tsx` | edit | Compliance donut + deadline + data blocks + recent orders | ~180 |
| `src/app/(dashboard)/exchange/page.tsx` | new | Order book table + filters + realtime listings subscription | ~220 |
| `src/app/(dashboard)/checkout/[listingId]/page.tsx` | new | Trust panel + pricing breakdown + simulate transfer | ~180 |
| `src/app/(dashboard)/orders/[id]/certificate/page.tsx` | new | Provisional certificate render + download | ~140 |
| `src/lib/actions/buyer.ts` | new | Server actions: create profile, save liability, place order | ~160 |
| `src/components/compliance-donut.tsx` | new | Animated donut (Red→Amber→Blue→Green) | ~90 |

### Phase 3 — Seller flow
| Path | Action | Purpose | LOC |
|---|---|---|---|
| `src/app/onboarding/seller/page.tsx` | new | Onboarding + verification loader + doc upload | ~180 |
| `src/app/seller/vault/page.tsx` | new | Category metrics + listings + incoming orders (realtime) | ~200 |
| `src/app/seller/listings/new/page.tsx` | new | Create listing form + dynamic pricing assistant + preview | ~180 |
| `src/app/seller/orders/[id]/page.tsx` | new | Escrow handshake: 48h timer, GSTIN, CPCB guide, accept/decline | ~190 |
| `src/lib/actions/seller.ts` | new | Server actions: create profile, publish listing, accept/decline | ~160 |

### Phase 4 — AI features
| Path | Action | Purpose | LOC |
|---|---|---|---|
| `src/app/api/copilot/route.ts` | new | Grounded copilot: inject deficit + live listings, recommend buys | ~90 |
| `src/components/copilot-panel.tsx` | new | Chat panel on dashboard + exchange, deep-links to checkout | ~140 |
| `src/app/api/extract-cert/route.ts` | new | Claude doc understanding → extract CPCB reg no + capacity | ~80 |
| `src/app/api/estimate-liability/route.ts` | new | `generateObject` NL business desc → kg by category | ~70 |

### Phase 5 — Demo e2e
| Path | Action | Purpose | LOC |
|---|---|---|---|
| `e2e/realtime-sync.spec.ts` | new | Two-context test = the 90s demo (the integration test) | ~120 |
| `e2e/auth-guards.spec.ts` | new | Unauthed access blocked; cross-tenant order access denied | ~80 |

## Data model (summary)
- `brands`(id, clerk_user_id ⚷, company_name, gstin, contact_name, phone, email, state)
- `recyclers`(id, clerk_user_id ⚷, company_name, cpcb_reg_no, state, capacity_mt, whatsapp, doc_url, verified)
- `liabilities`(id, brand_id→, category, market_kg, target_pct, liability_kg, unique(brand,category))
- `listings`(id, recycler_id→, category, qty_kg, price_per_kg, status[active|sold|partial], created_at)
- `orders`(id, buyer_id→, recycler_id→, listing_id→, category, qty_kg, price_per_kg, credits_cost, platform_fee, total, status[pending|transferred|declined|expired], expires_at, created_at)
- `certificates`(id, order_id→ ⚷, reference_id ⚷, issued_at)

**RLS (Clerk userId via JWT):**
- `listings`: SELECT for any authed user where `status='active'`; INSERT/UPDATE only owning recycler.
- `orders`: SELECT/`mutate` only where `buyer_id=me OR recycler_id=me` — closes order-book IDOR.
- `brands`/`recyclers`: own row full; recycler public fields exposed via a view for the order book.
- Realtime publication on `listings` + `orders`; subscriptions inherit RLS filtering.

## Test plan
| Behavior | Existing test | New test | Level |
|---|---|---|---|
| Liability = market×target per category | none | `liability.test.ts` | unit |
| Deficit = liability − secured | none | `liability.test.ts` | unit |
| Cost-range bands per category | none | `liability.test.ts` | unit |
| Platform fee = 5%, total | none | `pricing.test.ts` | unit |
| Match deficit → pool (cap at qty) | none | `matching.test.ts` | unit |
| Order transitions + 48h expiry | none | `orders.test.ts` | unit |
| Certificate reference-ID format/uniqueness | none | `certificate.test.ts` | unit |
| `/api/copilot` / `/extract-cert` / `/estimate-liability` auth guard | none | route tests | integration |
| Liability estimator returns valid category kg | none | `estimate.test.ts` (mock model) | integration |
| **Realtime: listing in A appears in B; order in B lights up A** | none | `realtime-sync.spec.ts` | e2e |
| Unauthed blocked; cross-tenant order denied | none | `auth-guards.spec.ts` | e2e |

## Risk surface
- **Next.js 16 breaking changes** — mitigation: read `node_modules/next/dist/docs/` before each server-action/route file; don't assume Next 14/15 APIs.
- **RLS misconfig silently breaks realtime** (subscriptions return nothing) — mitigation: explicit RLS e2e test + verify replication enabled per table.
- **Clerk→Supabase JWT template not configured** — mitigation: prerequisite checklist before Phase 0; `createUserClient()` already expects template `supabase`.
- **AI latency/cost in live demo** — mitigation: stream responses, keep copilot context small, prompt-cache system prompt, have a canned fallback.
- **`generateObject` structured-output flakiness** — mitigation: strict zod schema + retry + sane defaults.
- **Doc extraction on arbitrary PDFs** — mitigation: size/type cap, in-memory to Claude (no storage), zod-validate extracted fields.

## Rollback
Hackathon feature branches off `develop`. Each phase = its own `feature/*` branch → PR to `develop`. Rollback = don't merge, or `git revert` the merge commit (<5 min). No prod, no data migration risk. AI features are independent routes — disabling one is deleting its route + hiding its entry point.

## Open questions
1. **Supabase project** — none provisioned yet (`.env.local` placeholders). Need real URL + anon + service-role keys, and the **Clerk→Supabase JWT template** created in both dashboards. Blocks Phase 0. Can you provision, or want me to walk you through it?
2. **Clerk email verification** — disable for the demo so onboarding has "no email walls" per the doc? (Clerk dashboard setting.)
3. **Certificate output** — real downloadable PDF (adds a PDF lib) or styled print-to-PDF HTML page? Recommend HTML/print for speed.
4. **Doc extraction storage** — keep uploaded CPCB PDF in Supabase Storage, or process in-memory and discard? Recommend in-memory for the demo.
5. **Partial fills** — if a listing's qty < buyer deficit, cap the order at listing qty and leave residual deficit? (Assumed yes in `matching.ts`.)
