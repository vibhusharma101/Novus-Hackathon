# Review: Phase 4 — NL Liability Estimator

_Generated by /vii-review on 2026-06-13. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 55 passing (7 files), 0 failures — no regressions.
- Typecheck: `npx tsc --noEmit` → clean.

## Plan alignment
- Plan (Phase 4): `src/app/api/estimate-liability/route.ts` — "`generateObject` NL business desc → kg by category". ✅ Delivered exactly.
- Added: `src/components/calculator/ai-estimator.tsx` (the UI card) + `wizard.tsx` wiring (`applyEstimate`, Step 1 injection). Required to surface the feature.
- Verdict: **on-plan**

## Findings

### BLOCKER
_None._

### MAJOR
_None._

### MINOR

- `src/app/api/estimate-liability/route.ts` — **No per-user rate limit on a paid LLM endpoint** (same cost/DoS class as `/api/copilot` and the pre-existing `/api/chat`). The body is capped at 2,000 chars and `generateObject` output is small, so the blast radius is smaller than the streaming copilot, but it's still an unthrottled paid call. Documented in `.vii/security/p4-copilot.md`; add a shared rate limiter before production.

- `src/components/calculator/ai-estimator.tsx` — **All-zero estimate shows "Pre-filled below" but selects nothing.** If the model returns 0 for every category (vague/irrelevant description), `applyEstimate` clears the selection and the success banner is misleading. Cheap improvement: if all three are 0, show a "couldn't infer volumes — pick categories manually" hint instead of the success state.

### NIT
- `applyEstimate` in `wizard.tsx` is small pure logic (estimate → selected+weights) that could be unit-tested, but it's UI-coupled and matches the screens' no-unit-test precedent. Not blocking.

## Auto-fixes applied
- None — imports across all three files are used; no dead code. (The earlier awkward `EXAMPLES.map().slice(0,1)` was cleaned up during the build into two labelled example chips.)

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR. The estimator is auth-gated, schema-constrained (`generateObject` + Zod), server-clamped, and pre-fills the calculator at the most useful point (Step 1, driving both category selection and weights). MINORs are the shared rate-limit hardening and an all-zero UX polish.
