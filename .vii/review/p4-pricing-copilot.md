# Review: Phase 4 — Seller Pricing Copilot

_Generated by /vii-review on 2026-06-13. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 55 passing (7 files), 0 failures.
- Typecheck: `npx tsc --noEmit` → clean.

## Plan alignment
- User-selected Phase 4 feature (not in the original plan.md list, which named copilot/estimate/extract). On-plan per the user's explicit Phase 4 scope choice.
- Files: `src/app/api/suggest-price/route.ts` (new), `src/components/seller/create-listing-form.tsx` (AI suggestion button + result in the Market Insights aside).
- Verdict: **on-plan**

## Findings

### BLOCKER
_None._

### MAJOR
_None._

### MINOR

- `src/app/api/suggest-price/route.ts:68` — **`Math.min(...prices)` spread** on the category's active-listing prices. Bounded by one category's active listings (small at demo scale); same unbounded-spread class noted for B5/S3. Switch to a `reduce` min if active volume ever grows large.

### NIT

- `src/components/seller/create-listing-form.tsx` — `suggestPrice` is declared just above the `qtyKg` `useMemo` it closes over. Functionally correct (the closure reads `qtyKg` lazily at click time, long after initialization), and tsc is clean, but it reads slightly out of order. Could move the handler below the derived values for clarity.
- `src/app/api/suggest-price/route.ts` — no recycler-role check; a buyer could also call it. Harmless — it returns only public-market pricing advice (no data read beyond public active listings, no write).

## Auto-fixes applied
- None — `Sparkles` import added and used; no dead code.

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR. The pricing copilot is auth-gated + rate-limited, grounded in the live per-category market, and **clamps the model's price to a sane band** before it reaches the input. It augments (doesn't replace) the existing rule-based assistant, and the suggested price flows through the live verdict/payout. NITs are cosmetic.
