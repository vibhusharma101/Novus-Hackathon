# Review: Phase 4 — Buyer Copilot

_Generated by /vii-review on 2026-06-13. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 55 passing (7 files), 0 failures — no regressions.
- Typecheck: `npx tsc --noEmit` → clean (after confirming AI SDK v6 surface: `convertToModelMessages` is async, `useChat`/`DefaultChatTransport` shapes).

## Plan alignment
- Plan (Phase 4): `src/app/api/copilot/route.ts` ✅, `src/components/copilot-panel.tsx` ✅ (built at `src/components/copilot/copilot-panel.tsx`).
- `(dashboard)/layout.tsx` — mounts the panel across all buyer screens. On-plan (the plan says "panel on dashboard + exchange").
- Verdict: **on-plan**

## Findings

### BLOCKER
_None._

### MAJOR
_None._

### MINOR

- `src/app/api/copilot/route.ts` — **No rate limit or per-message length cap on the LLM endpoint.** Messages are capped at 40 by count, but individual message text length isn't bounded, and there's no per-user throttle — an authenticated buyer could spam costly Anthropic calls. Consistent with the existing `/api/chat` precedent (also unthrottled); flagged for production hardening (add a per-user rate limit + a total-input-token guard).

- `src/components/copilot/copilot-panel.tsx:23` — **`renderText` (the `[[buy:id]]` marker parser) is untested.** It returns React nodes so it's UI-coupled, but the parsing is the demo-critical bit (broken parse = no Buy buttons). Consider extracting a pure `parseBuyMarkers(text) → segments[]` and unit-testing it. Left as-is to match the established no-unit-test-for-UI precedent of the screen components.

### NIT

- `src/components/copilot/copilot-panel.tsx` — the text input has a placeholder but no associated `<label>`/aria-label (the launcher button does). Minor a11y gap.

## Auto-fixes applied
- None — imports in both files are all used; no dead code.

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR. The copilot is grounded server-side in the buyer's own liability + the public live market, auth-gated, and emits checkout deep-links that the checkout page independently validates. MINORs are production-hardening (rate limit) and a test-coverage suggestion.
