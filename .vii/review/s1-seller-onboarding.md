# Review: S1 — Seller Onboarding

_Generated by /vii-review on 2026-06-12. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 41 passing before review; **49 passing** after adding 8 seller-validator tests. 0 failures.
- Typecheck: `npx tsc --noEmit` → clean (after fixing the `z.coerce` resolver-type issue, see below).

## Plan alignment
- Plan (Phase 3): `src/app/onboarding/seller/page.tsx` ✅, `src/lib/actions/seller.ts` ✅ (`createRecyclerProfile`; the other seller actions land in S3/S4).
- Added beyond plan: `src/lib/validators/seller.ts`, `src/components/onboarding/seller-form.tsx` (form split, mirrors buyer pattern), `supabase/schema.sql` (`contact_name` column), `sign-up` redirect wiring. All directly required by S1.
- Verdict: **on-plan**

## Findings

### BLOCKER
_None._

### MAJOR
_None (pre-empted — see auto/added tests)._

### MINOR

- `src/components/onboarding/seller-form.tsx:52` — **Happy path routes to `/seller/vault` (S2), which does not exist yet.** After the simulated verification, `router.push('/seller/vault')` will 404 until S2 ships. This is the sequential-build pattern used throughout (B6→B7); S2 is the immediate next screen. Not blocking the commit, but the seller flow is not end-to-end runnable until S2 lands.

- `src/components/onboarding/seller-form.tsx:175` — **Document upload is cosmetic.** The dropzone captures the filename for display but never uploads the file, and `doc_url` is left null by the action. This is intentional per the spec ("simulated verification") — no Supabase Storage bucket is provisioned. Documented here so it isn't mistaken for a wired upload. The field is not marked required, which is consistent.

- `src/lib/actions/seller.ts:42` — **`verified: true` is set unconditionally (simulated).** Server-side only (client cannot set it), and explicitly commented as a demo simulation. Correct for the hackathon; flagged so a real deployment replaces it with an actual document-verification step.

### NIT

- `src/lib/validators/seller.ts` mirrors `buyer.ts` closely (re-exports `INDIAN_STATES`, reuses `PHONE_REGEX`). Good reuse; no dead duplication.

## Auto-fixes applied
- None. (The `z.coerce.number()` → `z.number()` + `valueAsNumber` change was a typecheck fix made during the build, not a review-stage auto-fix; the 8 added validator tests were a deliberate pre-emption of the "untested new behavior" MAJOR.)

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR. Three MINORs are all intentional, documented hackathon trade-offs; the `/seller/vault` 404 resolves when S2 ships next. (`/vii-cso` flagged + fixed one MEDIUM in the same commit: column-level grant locks recycler private fields — see `.vii/security/s1-seller-onboarding.md`.)
