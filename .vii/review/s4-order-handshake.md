# Review: S4 — Order Handshake

_Generated by /vii-review on 2026-06-13. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 55 passing (7 files), 0 failures — no regressions.
- Typecheck: `npx tsc --noEmit` → clean (after `next typegen` for the new `/seller/orders/[id]` route).

## Plan alignment
- Plan (Phase 3): `src/app/seller/orders/[id]/page.tsx` ✅, `respondToOrder` in `src/lib/actions/seller.ts` ✅.
- Added: `src/components/seller/order-handshake.tsx` (client). Required by S4.
- Verdict: **on-plan** — completes the Phase 3 seller flow (S1–S4).

## Findings

### BLOCKER
_None._

### MAJOR
_None._

### MINOR

- `src/lib/actions/seller.ts` (`respondToOrder`) — **No unit test.** Consistent with the established server-action precedent (`placeOrder`, `createRecyclerProfile`, `createListing` are all integration-level and DB-bound, not unit-tested). The one piece of pure logic it relies on — `provisionalCertificateId` — is already covered, and the accept→transferred path is part of the Phase 5 demo e2e. Acceptable given precedent; flagged so it's tracked for the e2e.

- `src/lib/actions/seller.ts` — **Cert-insert failure after a successful status update returns an error while the order is already `transferred`.** If the `certificates` insert fails with a non-`23505` error, the order is transferred but the action returns `{ ok: false }`, so the client stays on `pending` until reload (which would then show transferred). Honest (not swallowed) and rare; for the demo the insert won't fail. Noted as an at-most-cosmetic inconsistency.

### NIT

- `src/components/seller/order-handshake.tsx` — the accept button's `disabled={isPending || !isPendingOrder}` includes a redundant `!isPendingOrder` (that branch only renders when the order is pending & not expired). Harmless belt-and-suspenders; left as-is.

## Auto-fixes applied
- None at review time. (During the build, the invalid `TaskAlt` lucide import — a Material Symbols name — was swapped for `CheckCheck`.)

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR. `respondToOrder` is correctly layered (auth → recycler confirm → ownership → pending → expiry → race-guarded `.eq('status','pending')` update → deterministic cert issue), and the client renders every terminal state (transferred / declined / expired). Accept→transferred flows live to the seller's own vault via the S2 realtime subscription. (`/vii-cso` flagged + fixed one MEDIUM in the same commit: tightened the `certificates` insert RLS policy to the order's recycler — see `.vii/security/s4-order-handshake.md`.)
