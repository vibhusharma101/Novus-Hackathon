# Review: B5 — Live Exchange / Order Book

_Generated by /vii-review on 2026-06-11. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 36 passed (5 files), 0 failures — pre-existing baseline, no regressions in this diff.
- Lint/typecheck: `npx tsc --noEmit` → clean both before and after auto-fix.

## Plan alignment
- Plan path `src/app/(dashboard)/exchange/page.tsx` matches actual URL `dashboard/exchange`; file lives at `src/app/(dashboard)/dashboard/exchange/page.tsx` (plan omitted the `dashboard/` nesting). Not scope drift — same intent, same URL, just file-path difference.
- `src/components/exchange/order-book.tsx` not explicitly in plan — plan listed `exchange/page.tsx` but a client component split is the correct implementation pattern for realtime. On-plan.
- `supabase/schema.sql` — `"recyclers: public read"` policy added in prior commit to enable browser client recycler fetch. On-plan.
- Verdict: **on-plan**

## Findings

### BLOCKER
_None._

### MAJOR
_None._

### MINOR

- `src/components/exchange/order-book.tsx:451` — **Pagination shows pages 1–5 only (not a sliding window).** If `totalPages > 5` and the user navigates to page 8, the page-number buttons still show 1–5, making pages 6+ unreachable except via the Previous/Next arrows. For a hackathon demo this is acceptable, but real data with >50 listings will hit it. Fix: compute `startPage = Math.max(1, page - 2)` and slice from there.

- `src/components/exchange/order-book.tsx:532–535` — **"Details" mobile button is a no-op stub.** The button renders with no `onClick`; pressing it does nothing. Users may expect a detail sheet or navigation. Fine for B5 scope (checkout is B6), but should get an `onClick={() => handleBuy(listing.id)}` or be removed before demo to avoid confusion.

### NIT
_None._

## Auto-fixes applied
- `src/components/exchange/order-book.tsx:153` — realtime recycler SELECT was missing `capacity_mt`; `PublicRecycler` type requires it as `number`, so the `as PublicRecycler` cast was hiding a runtime `undefined`. Added `capacity_mt` to the select field list.

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR. Two MINOR items; pagination is a known limitation, Details stub is a B6 placeholder.
