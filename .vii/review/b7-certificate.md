# Review: B7 — Order Confirmation & Certificate

_Generated by /vii-review on 2026-06-12. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 36 passed before review; 41 passed after adding `provisionalCertificateId` tests (+5). 0 failures.
- Typecheck: `npx tsc --noEmit` → clean.

## Plan alignment
- Plan: `src/app/(dashboard)/orders/[id]/certificate/page.tsx` → built at `src/app/(dashboard)/dashboard/orders/[id]/certificate/page.tsx` (same dashboard nesting as B4–B6). On-plan.
- `src/components/certificate/order-certificate.tsx` — client cert render (donut + print). On-plan (implied by the page).
- `src/lib/epr/certificate.ts` — added `provisionalCertificateId` (deterministic). Reuses existing module.
- `src/app/globals.css` — print-isolation `@media print` rule. Not in plan; trivial enabler for "Download as PDF (print-to-PDF fallback)".
- Files in plan but not in diff: none for B7.
- Verdict: **on-plan**

## Findings

### BLOCKER
_None._

### MAJOR
_None (resolved during review — see below)._

- ~~`provisionalCertificateId` shipped without a test~~ → **Resolved:** added 5 unit tests covering format, determinism, seed-derivation, `parseCertificateId` round-trip, and short-seed padding.

### MINOR

- `src/components/certificate/order-certificate.tsx:118` — **Donut delta label hides "Compliant" once a delta exists.** The center shows `+{deltaPct}% Δ` when `deltaPct > 0`, else `Compliant`. Since every reached certificate has `deltaPct > 0` (the order contributed volume), the word "Compliant" effectively never renders. Cosmetic; matches the desktop design which shows the delta. Left as-is.

- `src/app/(dashboard)/.../certificate/page.tsx:50` — **`acquiredKg` counts `pending` + `transferred` orders.** A provisional/pending order inflates "acquired" before the seller confirms transfer. This is intentional per the chosen semantics (donut increments on purchase, spec-aligned) but means the compliance % is optimistic until S4 handshake. Acceptable for the buyer-flow demo; worth revisiting when S4 lands so declined/expired orders correctly subtract.

### NIT

- `src/components/certificate/order-certificate.tsx` — Implemented as a single responsive layout (stacks on mobile) rather than two distinct desktop/mobile trees like B5/B6. The mobile design's standalone "Transaction Status / EXECUTED" card is folded into the shared success header. Renders correctly at mobile widths; flagging only as a deviation from the per-breakpoint pattern used elsewhere.

- `src/components/certificate/order-certificate.tsx:151` — Signature uses `fontFamily: 'cursive'` inline (design used an image asset). Pragmatic substitute; fine for hackathon.

## Auto-fixes applied
- None (test addition was a deliberate MAJOR resolution, not a trivial auto-fix).

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR (the missing-test MAJOR was resolved with 5 new passing tests). Two MINOR items are intentional semantics, documented for the S4 follow-up.
