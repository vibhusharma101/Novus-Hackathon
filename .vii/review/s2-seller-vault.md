# Review: S2 — Inventory Vault

_Generated by /vii-review on 2026-06-13. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 49 passing (6 files), 0 failures — no regressions.
- Typecheck: `npx tsc --noEmit` → clean (after `next typegen` regenerated stale route types for the new `/seller` segment, and after removing 2 unused imports).

## Plan alignment
- Plan (Phase 3): `src/app/seller/vault/page.tsx` ✅ (the plan listed `/seller/vault` with realtime incoming orders).
- Added beyond plan: `src/app/seller/layout.tsx` + `seller-shell.tsx` (seller shell — no seller layout existed), `seller-vault.tsx` (client), plus enabling schema/action changes (orders denormalized buyer fields, listings own-read policy, `placeOrder` populating the new fields). All directly required to make orders consumable by the seller.
- Verdict: **on-plan** (with justified supporting changes)

## Findings

### BLOCKER
_None._

### MAJOR
_None._

### MINOR

- `src/components/seller/seller-vault.tsx:118` — **`initialListings` is a static prop; listing metrics/table don't update on realtime.** Only `orders` is subscribed. If a listing sells out or a new listing is created elsewhere, the vault's tiles/table won't reflect it until reload. Intentional for S2 scope (the realtime requirement is on incoming orders, which is wired); listings refresh on navigation. Noted for S3 (after creating a listing, the seller returns to a freshly-rendered vault).

- `src/components/seller/seller-vault.tsx` — **Incoming-order "Review & Respond" and "New Listing" route to S4/S3, which don't exist yet** (`/seller/orders/[id]`, `/seller/listings/new`). Same sequential-build pattern as earlier screens; both resolve as those screens land next.

- `supabase/schema.sql` / `src/lib/actions/buyer.ts` — **`buyer_gstin` + `buyer_company_name` denormalized onto `orders`.** Correct call: the seller cannot read the buyer's `brands` row under RLS, and a self-contained order row is ideal for the realtime INSERT payload. Trade-off: if a buyer later edits their company name/GSTIN, historical orders keep the value at order time — acceptable (arguably desirable for an audit record).

### NIT

- `src/components/seller/seller-vault.tsx:96` — `metrics` recomputes over all listings + orders on every `orders` change. Fine at demo scale (handful of rows); would want memo-by-id bucketing only at thousands of orders.

## Auto-fixes applied
- `src/components/seller/seller-vault.tsx` — removed unused `Check`, `X` lucide imports (left over from when Accept/Decline were inline before routing to S4).

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR. MINORs are intentional scope boundaries (realtime is correctly wired on the incoming-orders requirement); the S3/S4 route targets resolve as those screens ship next.
