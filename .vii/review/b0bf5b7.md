# Review: b0bf5b7

_Generated by /vii-review on 2026-06-07. Base: initial commit (all code uncommitted). Plan: none — .vii/plan.md missing._

## Baseline
- Tests: NO TEST SUITE — no test script in package.json, no test files found
- Lint: PASS (ESLint — no errors)
- TypeScript: PASS (tsc --noEmit — clean)

## Plan alignment
- No `.vii/plan.md` exists — review proceeding without a plan (reduced confidence)
- All files are new/uncommitted work scaffolded on top of the initial Create Next App commit

## Findings

### BLOCKER

- `src/app/api/chat/route.ts:1-14` — **AI endpoint has no authentication check.** Any unauthenticated internet user can POST to `/api/chat` and consume your Anthropic API credits without limits. The middleware protects page routes but `/(api|trpc)(.*)` routes require explicit `auth.protect()` inside the handler. Fix: call `await auth()` and check `userId` before calling `streamText`.

### MAJOR

- `src/lib/supabase.ts:4` — **Service role key bypasses all Row Level Security.** The Supabase client is initialized with `SUPABASE_SERVICE_ROLE_KEY`, which ignores RLS policies entirely. Any future bug in the application's authorization layer (e.g., forgetting to filter by `user_id`) will silently expose every user's data. Fix: use the anon key + per-request auth context for user-scoped queries; reserve the service role key for admin/background jobs only.

- `src/app/api/chat/route.ts:5` — **No input validation on `messages`.** The handler reads `messages` directly from the request body with no shape check. Malformed payloads (non-array, missing `role`/`content`, excessively large body) will either throw unhandled errors or pass junk to the AI SDK. Fix: validate with zod before use.

- `src/app/(dashboard)/error.tsx:13-15` — **Raw `Error` object logged to console in production.** `console.error(error)` in a client component will print full stack traces (and potentially sensitive data in error messages) to the browser console in production builds. Fix: log a sanitized string or use a proper error monitoring service (e.g. Sentry).

### MINOR

- `src/lib/actions.ts:31` — **Unnecessary re-exports of Next.js internals.** `export { revalidatePath, redirect }` re-exports framework functions that every file should import directly. This creates a misleading indirection and will silently break if callers expect these to behave like server actions.

- `src/app/page.tsx:15` — Placeholder comment left in shipped code: `{/* Replace with real tagline once product spec is ready */}`. Remove or replace before any real users see it.

### NIT

- `src/app/sign-in/[[...sign-in]]/page.tsx:1` — `'use client'` directive is required here (Clerk's `<SignIn />` is a client component), but it could be a server component wrapper with a client child to keep the page shell server-rendered. Very low priority.

## Auto-fixes applied

None — all findings above are semantic.

## Verdict

**DO-NOT-SHIP**

1 BLOCKER (unauthenticated AI endpoint), 3 MAJOR findings. Fix the BLOCKER in `src/app/api/chat/route.ts` before any public exposure.
