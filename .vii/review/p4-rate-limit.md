# Review: Phase 4 — AI endpoint rate limiting (CSO fix)

_Generated by /vii-review on 2026-06-13. Base: main. Plan: .vii/plan.md (security follow-up)._

## Baseline
- Tests: 55 passing (7 files), 0 failures.
- Typecheck: `npx tsc --noEmit` → clean.

## Plan alignment
- Not in the original plan — this is the fix for the recurring MEDIUM flagged by `/vii-cso` across `/api/copilot`, `/api/estimate-liability`, and `/api/chat`.
- Verdict: **on-plan** (resolves a tracked security finding)

## Findings

### BLOCKER
_None._

### MAJOR
_None._

### MINOR

- `supabase/schema.sql` (`ai_rate_limits`) — **No cleanup/TTL on the counter rows.** One row accumulates per user per 60s window and is never deleted, so the table grows unbounded over time. Negligible at demo scale; for production add a periodic `delete from ai_rate_limits where window_start < now() - interval '1 day'` (cron) or prune opportunistically inside the function.

- `src/lib/rate-limit.ts` — **No unit test.** The fixed-window logic lives in SQL (`check_ai_rate_limit`) and the helper is a thin `rpc` wrapper with documented fail-open; consistent with the repo's no-unit-test precedent for infra/route glue. Flagged for awareness.

### NIT
- Fixed-window (not sliding) allows a boundary burst (up to 2× the limit across a window edge). Acceptable for a cost-DoS control — sustained rate is still bounded to 30/min. Documented, not a defect.

## Auto-fixes applied
- None — all three route guards import and use `checkRateLimit`; no dead code.

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 MAJOR. The limiter is atomic (row-locked on-conflict increment), shared across instances (Postgres-backed), server-only (RLS-locked table + `service_role`-only EXECUTE), and fail-open on infra error so it can't take the app down. Guards reject before any LLM call in all three routes.
