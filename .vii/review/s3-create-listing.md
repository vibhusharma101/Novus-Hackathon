# Review: S3 — Create Listing

_Generated by /vii-review on 2026-06-13. Base: main. Plan: .vii/plan.md._

## Baseline
- Tests: 49 passing before; **55 passing** after adding 6 `listingSchema` tests. 0 failures.
- Typecheck: `npx tsc --noEmit` → clean (after `next typegen` for the new `/seller/listings/new` route).

## Plan alignment
- Plan (Phase 3): `src/app/seller/listings/new/page.tsx` ✅, `src/lib/actions/seller.ts` (`createListing`) ✅ — both match.
- Added: `src/lib/validators/listing.ts` (+test), `src/components/seller/create-listing-form.tsx`. Directly required by S3.
- Verdict: **on-plan**

## Findings

### BLOCKER
_None._

### MAJOR — resolved during review
- ~~`create-listing-form.tsx` publish strip overlapped the seller layout's mobile bottom-nav~~ — both were `fixed bottom-0` on mobile (strip z-30 under nav z-40), partially obscuring the publish button. **Fixed:** strip is now `bottom-16 lg:bottom-0` (sits above the h-16 mobile nav on small screens, at the viewport bottom on desktop) and content padding bumped to `pb-40 lg:pb-28` for clearance.

### MINOR
- `src/app/seller/listings/new/page.tsx:36` — **Unbounded active-listings fetch + `Math.min(...prices)` spread.** The pricing assistant reads all active listings (no `LIMIT`) and spreads the price array into `Math.min`. Fine at demo scale; at tens of thousands of active listings the fetch is heavy and the spread risks an arg-count limit. Switch to a `.reduce` min + a capped/aggregated query when volume grows. (Same unbounded-fetch class as B5.)

- `src/components/seller/create-listing-form.tsx` — **"Save as Draft" omitted.** The desktop design has a secondary "Save as Draft" button; there's no draft status in the schema (`listings.status ∈ active|partial|sold`), so it was intentionally dropped rather than faked. Noted as a deliberate scope decision.

### NIT
- `verdict`/`deltaPct` recompute on each keystroke via `useMemo` — negligible, correct.

## Auto-fixes applied
- `src/components/seller/create-listing-form.tsx` — swapped invalid `Insights` lucide import (Material Symbols name) for `LineChart` during the build.

## Verdict
**SHIP-READY** — 0 BLOCKER, 0 unresolved MAJOR (the mobile overlap was fixed in-review). S3 completes the live-demo producer path: publish → `listings` INSERT → realtime broadcast to buyer order books.
